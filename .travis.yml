language: java

before_script:
  - chmod +x ./gradlew

script:
  - ./gradlew build

deploy:
  provider: script
  script: ./gradlew build :bintrayUpload
  skip_cleanup: true
  file: bintray
  user: $BINTRAY_USER
  key: $BINTRAY_KEY
  on:
    branch: master